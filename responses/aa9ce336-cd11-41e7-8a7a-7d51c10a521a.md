I agree, a passcode is the way to go for now. I'll implement a simple login page that stores a session cookie. This will keep the interface clean while ensuring only someone with the code can chat.

I'll set the default passcode to something like '1234' (which we can change later) and update the server and frontend to require it. 

Should I go ahead and implement this 'Gatekeeper' layer now?