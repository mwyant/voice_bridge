I've noted the cutoff issue. Currently, the webpage uses the browser's basic MediaRecorder API and treats the recording as a single file. There's no AI-based silence detection in the frontend yet, but it's possible that the browser's recording buffer or the file format is causing a truncation.

Specifically, I'm currently telling the browser to label the audio as a WAV file, but browsers usually record in WebM format. This mismatch can sometimes confuse the transcription engine.

Shall I update the frontend to use a more robust recording method, or perhaps add a visual volume meter so you can see if it's still capturing your voice?