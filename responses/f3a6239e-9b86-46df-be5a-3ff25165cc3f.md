That makes a lot of sense. To make this a standard part of your toolkit, I'll create a dedicated launch script. 

I propose moving the entire 'voice_chat_system' into your '.opencode\tools\voice_bridge' folder. This aligns with your standard layout and makes it easy for any OpenCode session to find and launch it.

I'll start by creating a 'launch_voice.ps1' script that:
1. Checks for the required Python environment.
2. Generates certificates if they are missing.
3. Launches the server and bridge in the background.

Shall I go ahead and migrate the files to your '.opencode' directory and set up that launch script?